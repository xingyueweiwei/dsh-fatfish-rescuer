# 📋 版本说明 · 大肥鱼救星

## ✅ v5.0.2（2026-09-22 · **建议发这一份**）

> 把「插件冲突治理」与「防误报加固」两路成果合成**同一份字节**，并修掉一个实测出来的真缺陷。
> 用过 v5.0 / v5.0.1 的人都**值得换**这一版。

### 新增：🧩 插件打架体检（冲突雷达，全部只读）

| 命令 | 作用 |
|---|---|
| `--conflict` | 共享物占用表 + 判红 + **覆盖面自报**（有真红时退出码 1 是**正确行为**，不是报错） |
| `--conflict-plan` | 对每条真红给处置预演：改哪 / 等价命令 / 复验判据 / 回滚方式，**一个字节都不写** |
| `--conflict-selftest` | 判据自检 **31 项**（主 14 + 补充 10 + WP2 静态 7），全在临时目录里跑 |
| `--conflict-tiers` | 档位表（all/core/minimal），**只列成员、不起进程**（自报 `booted_here=0`） |

本机实测：**真红 0 条 / 提醒 39 条**（提醒不计入 `conflicts=`，报告里同时印"未扫类别 + 原因"）。
⚠️ **界面没有对应按钮**：真动手的处置（`--conflict-disable`）**尚未实现**，所以这四项都是命令行能力。

### 修复：按 PID 杀进程树只杀根、子进程全漏

**现象**：`--killtree-plan <一棵两层树>` 只报「会动 1 个」，子进程全被跳过，理由写着「命令行已经变了（PID 可能已被系统回收）」。

**根因**：`RunCapture` 用 `AppendLine` 拼子进程输出 ⇒ `QueryCmdLine` 读回的字符串**尾部带 CRLF**；而 `QueryChildren` 登记时 `Trim` 过 ⇒ 两边口径不一致 ⇒ 那条「命令行变了 ⇒ 跳过」的身份守卫**对每一个子进程都误命中**。（根进程两边同源所以照杀 —— 正好解释"只有根被杀"。）

**修法**：读回来的命令行先 `Trim` 再比。

**影响面**：`PatchLock`（修复 / 研究插件装包后清理）与 `PluginScout`（插件研究）的"杀干净"此前退化成只杀根 ⇒ 会留孤儿（端口占用、`node_modules` 半装、锁文件残留）。

**判据**：新增 `v5\killtree_judge.ps1` + 回归第 13 项 —— 真造一棵两层树，**会动数必须 ≥2**；预演后替身必须全部存活；树外进程不许出现；已退出的 PID 不许报成会动。**自带负对照**：拿修前的 exe 跑同一判据必须报红。

### 自证（可复核）

| 判据 | 结果 |
|---|---|
| `--selftest` | 失败 **0 / 93**（含页签 UserPaint 负对照「硬塞必须被抓住：抓住了」） |
| `--conflict-selftest` | `[OK] 31 / [FAIL] 0` |
| `--conflict` | 真红 **0** / 提醒 39，退出码 0 |
| `--conflict-tiers` | 三档齐全、`booted_here=0`（声明自己没起过进程） |
| `v5\killtree_judge.ps1` | **9 项全过**；同一判据跑修前 exe ⇒ **B1 报红、exit 1** |
| 版本号四处 | 源码 / README / 本文件 / exe 元数据 = **5.0.2** |

---

## ✅ v5.0.1（2026-09-20 · 热修 · 已被 v5.0.2 取代）

> 只修一个**误报**，没有新功能；用 v5.0 的人**值得换**这一版。

### 修了什么

**现象**：点「🚀 启动并打开」，界面报 **"★ 没起来 / 启动失败（日志已定性）"**，可 DSH 其实**正在正常冷启动**，过一会儿页面自己就好了。

**根因**：启动后第 5 秒会做一次"快速失败"判断，而它只读**日志文件的最后 6000 字节**；这两个日志是**只追加**的历史文件（跨几十次启动），里面躺着旧崩溃记录。本机实测：那次启动时，文件尾 **142 字符**处就是一段**旧**的 `EADDRINUSE 127.0.0.1:3199`，于是被判成"端口被占 ⇒ 启动失败"。它和本次要起的 3080 端口**毫无关系**。

**修法**：起进程**之前**先记下两个日志的字节长度当"本次起点"（并写一行带**时间戳**的分界），归因**只看起点之后新增的内容**；如果本次之后什么都没写，就如实说"没有新增内容"，**绝不再贴旧内容**当原因。

### 自证（可复核）

| 判据 | 结果 |
|---|---|
| `--selftest` 新增 5 条（含正负样本） | **0 / 91 失败** |
| 端到端回归 `v5\faults\e2e_stale_log_falsefail.ps1`（沙箱 home + 假 dsh + 从真实日志抠出的旧崩溃块，新旧 exe 同一输入 A/B） | **旧版 6.1 秒误报"没起来"；新版 10.8 秒正常 `OK（9875 ms）`** |
| 真实日志副作用 | 从此每次启动写一行时间戳分界（此前 `.err` 无时间戳） |

---

## v5.0（2026-09-20）

> 这一版**不是补丁**，是三块新能力 + 一次"假绿清算"。所以版本号直接从 v4.5.4 跳到 **v5.0**。

### 一句话

| 你会用到的 | 一句话 |
|---|---|
| 🔍 **研究插件**（新按钮） | 插件因为"要构建授权"装不上时，先给你一份**预演**（改哪个文件、加哪几行、怎么回滚），你点头才动手 |
| 🛡 **只诊断 / `--plan-kill`** | 怕它乱动就把闸门拉上；想看它会杀谁就先跑预演（**一个都不杀**） |
| `--smart` | 一键智能启动：该打开的打开、该清的清、**端口被别的程序占就换端口（绝不杀别人）** |
| `--headless` | 出一份**能直接转发**的只读体检报告（不弹窗、退出码可判） |
| `--classify` | 别人发来日志，一句话告诉你"这是哪类故障、该点哪个按钮" |
| 209 个插件实测矩阵 | 哪些能装、哪些装不上、哪些装上就把 DSH 拖死，都有实测结论 |

### 1. 🔍 研究插件（本版主角）

有些插件装不上，日志里报 `ERR_PNPM_IGNORED_BUILDS` —— 意思是**这插件要在你机器上跑自己的构建脚本，
而宿主要求先"点名授权"**。它**不是坏插件**，别急着禁用它。

这个按钮做的事（**每一步都看得见**）：

1. 查这个包在 npm 上的最新版本；
2. 在**临时目录**里干装一次（**完全不碰你的 profile**），让 **pnpm 自己**报出"哪些包需要授权"
   —— 不猜、不查名单，所以换成任何插件都成立；
3. 打印**预演**：适用类别 / 前置条件 / 预演内容 / **★ 将改哪里**（哪个文件、加哪几行）/ 等价命令 / 复验判据 / 回滚方式；
4. 你点「确定」后才真动手：**先备份** `pnpm-workspace.yaml` 与 `pnpm-lock.yaml` → 写入 `allowBuilds` 段
   → 在真实 profile 里跑一次 `pnpm install` **复验** → 不通过就**自动回滚**（配置还原 + 再跑一次 install 还原 node_modules）。

**真实例子**：查 `dsh-better-sidebar`（在 209 个插件的实测矩阵里它是 `INSTALL_FAIL`），
它会告诉你需要授权的是 **`node-pty`**。
命令行同款：`大肥鱼救星.exe --scout dsh-better-sidebar`（只预演）；加 `--apply` 才真动手。

**边界（写在界面上，不是藏在这里）**：它**不会结束、不会重启**任何 dsh；
但它会在你的**真实 profile** 里跑一次 `pnpm install`，所以插件目录会按新配置重装一次。
🛡 只诊断开着时，它**一个字节都不写**。

### 2. ★ 本版修掉的三个"会害人"的真 bug（都有取证，不是自查自夸）

| # | 症状 | 根因 | 现状 |
|---|---|---|---|
| 1 | 「🔍 研究插件」按钮**界面上根本看不见** | 按钮对象建出来了，却**从没被放进任何页面**（"功能做好了但用户看不到"） | 已放进「🧩 插件与皮肤」页；并加了**机器判据**：41 个按钮字段里只要有 1 个没进界面就报红 |
| 2 | 回归脚本在**恰好 1 项失败**时给出**假绿** | 计数写的是 `(… Where-Object …).Count`，而 PowerShell 5.1 下**单个 `[pscustomobject]` 没有 `Count` 属性** ⇒ 求值为 `$null` ⇒ 结论行渲染成 `FAIL `(空)、`$null -gt 0` 为假 ⇒ **退出码 0**。有多项失败时反而正常 —— 也就是"只有一个问题时恰好失效" | 全部改成 `@(...).Count`；并加了**判据自检**（1 项失败也必须报红，且报告里直接印出旧写法的 `null`） |
| 3 | 隐私扫描**有命中时不打印覆盖面** | 文件数只在"0 命中"那条分支里才打印 ⇒ 报告里出现 `覆盖面= 个文件` 这种空值，而"空覆盖面"正是"闸门可能压根没跑"的样子 | 改成**无条件先报覆盖面**；回归项抓不到数字即判失败（fail-closed） |

> 顺带：`v5\faults\` 里的**原始证据**含 13 处真实用户名/路径 ⇒ 用**字节级替换**（不重编码、不动行尾）
> 脱敏后再交付，**原件归档**在工程 `_归档\v5原始证据_未脱敏_20260920\`（不进交付目录）。
> 受测对象自己也修了六处：超时只杀 `cmd.exe`（留下 pnpm 孤儿进程在真实 profile 里继续装）、
> stdout/stderr **管道死锁**、回滚不还原 `node_modules`、包名未校验（可注入）、无 CLI（判不了）、临时目录残留。

### 3. ★ 关于"会不会杀掉别的 dsh"——**会，但那是 v5.0 之前；已经修好了**

主人问的就是这句。逐条查代码后的答案：

- **直接杀**：不会。能结束进程的代码只有 6 处，要么只杀**它自己刚启动的子进程**，要么必须通过
  **身份校验**（进程名 node + 命令行入口指向 dsh 的 `bin.js`）；端口被别的程序占着一律**跳过**。
- **间接杀**：**v5.0 之前确实会** —— 身份扫描是**全机**的，而"按端口收窄"只在设了
  `DSH_PORT` / `BFF_DSH_HOME` 时才生效 ⇒ 日常使用下「停止服务 / 重启 / 强力自愈 / 一键修复」
  会把**机器上所有 dsh 实例**一起结束，包括你另起的那个。
- **v5.0 的修法**：默认就**按端口收窄**，只动当前端口上的实例；一个都没认出来才回退全机，
  且**必须把"这次是全机范围"写出来**。四个确认框现在都会先告诉你"会结束几个、PID 是多少、范围是什么"。
- **实测取证（阳性 + 阴性对照）**：造一个"命令行像另一个端口的 dsh"的替身进程（其实是 `node -e` 睡觉），
  默认 `--plan-kill` **不把它列进名单**并明说"另有 1 个 dsh 在别的端口上，本次一个都不会碰"；
  把 `DSH_PORT=3199` 设上再跑，它**出现在名单里** ⇒ 证明识别逻辑看得见它，"没进名单"是真的被排除，不是判据瞎了。

详细的逐条清单与取证过程写在 `安全说明.md` 第七节。

### 4. 其余（v5 系列陆续加的，一并保留）

- 无头只读排障 `--headless [--log <日志>] [--out <文件>] [--classify-only]`：报告落在
  `%USERPROFILE%\.dsh\big-fat-fish-rescuer\headless-report.txt`，`--classify-only` 是**纯函数模式**
  （同一份日志在任何机器上结论一致，附指纹 `CLASSIFY_FINGERPRINT=sha256:…` 供跨机比对）；
- 修复动作的形式化闭环 `--plan-fix all` / `--fix-apply <动作id>`：**8 个动作**，每个都有
  前置 / 预演 / **将改哪里** / 等价命令 / 复验判据 / 回滚方式；真执行时"写前快照 → 执行 → 复验"，不过即回滚；
- 分类器防退化 `--corpus-check`：真实语料 **362 条 / 14 类**（当前精确率 94.0%、召回率 21.5%），
  门槛写死在程序里，判据退化会直接 `exit 1`；
- 安全性总闸 🛡 只诊断（`--readonly on|off|status`）覆盖到**新增的写动作**；
- 自检规模：`--selftest` **86 项**、`--configtest` **21 项**、`--scout-selftest` **13 项**、
  一键回归 `v5\回归.ps1` **14 项**。

## ✅ v4.5.4（2026-09-18 · 建议发这一份）

> 在 v4.5.3 之上改了「🚀 启动并打开」：**一键智能**，不让用户去判断进程状态。

以前用户得自己搞清楚"有没有进程、端口被谁占着"；现在这一颗按钮自己分四种情况（**全程受 🛡 只诊断约束**）：

| 情况 | 它会怎么做 |
|---|---|
| 端口上有**健康的 dsh**（有 HTTP 响应） | **不重启它**，直接打开界面（最快、最不打扰） |
| 端口上有东西占着**但不响应**，且占用者**确认是 dsh**（卡死的残留） | 先把它清掉（**只清确认是 dsh 的进程**，杀前自动备份配置），再重新拉起，就绪后打开 |
| 端口**没人占** | 直接拉起，就绪后打开 |
| 占用者是**别的程序** | **绝不杀它** —— 自动换一个空闲端口启动，并把端口写进 `port.txt`（下次也用它），然后打开 |

**实测（本机 2026-09-18，预演模式 `--smart --dry`）**：
- 3080 上有健康实例 ⇒ 输出「**不动它**，直接给你打开界面」；
- 把 3188 用别的程序占住 ⇒ 输出「**不杀它**，我改用空闲端口 3189」；
- 打开 🛡 只诊断后 ⇒ 直接拒绝、不碰任何进程。

命令行等价物：`大肥鱼救星.exe --smart [--dry]`（`--dry` 只说要做什么，不做）。

## ✅ v4.5.3（2026-09-18 · **建议发这一份**）

> 在 v4.5.2（安全总闸）之上又补一条 —— 起因是复现了用户那句「反复报错 3080、过一会又好了、然后又崩、进不去网页版」。

1. **端口被占时不再去起注定失败的新实例**。实测：端口上已有东西占着时再去启动 dsh，新实例**必然秒退**并打
   `code: 'EADDRINUSE', syscall: 'listen', port: 3080` —— 屏幕上刷的就是这类报错。
   而真正让人"进不去网页版"的，是**先有一个占着端口却不响应的实例**（卡死的残留）：页面打不开 →
   用户/工具一遍遍试着启动 → 每次都被端口挡回来 ⇒ "反复报错、时好时坏"。
   现在救星遇到这种局面**不去起新实例**，而是把处境与下一步直接说清（实测 4.1 秒给出结论）：
   "端口被占着但不响应 HTTP；若是 dsh 残留就点「停止服务」，若是别的程序就换端口；想先看是谁占着用 `--plan-kill`"。
2. 附：v4.5.2 的**🛡 只诊断总闸**与 **`--plan-kill` 预演**、v4.5.1 的**启动后自验证**与**不再用管道牵 DSH** 全部保留。

## ✅ v4.5.2（2026-09-18 · 安全加固版，**建议发这一份**）

> 起因：主人反馈「装了之后把 dsh 杀了，这玩意太可怕了」。逐行查证后的**事实**是：
> 救星**没有任何自动杀进程的路径** —— 会动手的五颗按钮（重启服务 / 停止服务 / 关闭界面 / 一键修复 / 强力自愈）
> **每一颗都会先弹确认框**，文案里写明「界面会断开、对话可能中断」；杀之前还会校验身份
> （只结束命令行指向 `dsh` 的 `bin.js` 的 node 进程），端口被**非 dsh 程序**占用时明确**不杀**、只告知。
> 但"它有能力杀"这件事，用户应该能**锁死** ⇒ 这一版加了两样：

1. **🛡 只诊断（安全总闸）**：打开后，重启/停止/启动/一键修复**一律被拒绝，连进程都不碰**。
   开关就在「🩺 诊断」页的「打不开就点这两个」那一组里，状态一眼可见；也记在
   `%USERPROFILE%\.dsh\big-fat-fish-rescuer\readonly.txt`，下次启动还记得。
   命令行：`大肥鱼救星.exe --readonly on|off|status`。
2. **`--plan-kill` 预演**：**先看它要杀谁，再决定动不动手** —— 它只列出候选进程
   （PID、进程名、命中的命令行），并明确标注哪些会被跳过（不是 dsh 的一律跳过），**一个都不杀**。

**实测（本机，2026-09-18）**：
- `--plan-kill` 点名了端口 3080 上那个 node 进程（PID 34812），并写明"本次一个都没杀"；
- 开总闸后 `--start` **退出码 1**、回「只诊断模式已开：救星不会碰任何 dsh 进程 —— 本次动作已取消」；
- **实验前后 3080 上的 dsh PID 完全相同** ⇒ dsh 毫发无伤；
- 新增自动判据（`--selftest` 里一条）：「开启总闸后 StartDsh 必须被拒绝」。

## ✅ v4.5.1（2026-09-18 · 在 v4.5 之上的补丁版）

> v4.5 已封版并保持原样；这一版只补四件事，**建议发这一份**。
> 怎么看自己是哪版：窗口标题栏、或 `--selftest` 第一行 —— 会写 `v4.5.1`。

1. **启动后自己验证**：以前只要把进程拉起来就报「已启动」，而 DSH 在**插件树加载失败**时会自己退出
   —— 于是「救星说启动好了、页面却死活打不开」。现在起完必须确认端口真有 HTTP 响应；
   起不来就读 web 日志、按与 `--classify` 同一套判据说清**是哪个插件、缺什么、该点哪个按钮**；
   日志里一出现已知故障**立刻定性**（实测端口被占场景 6.7 秒给结论），慢机器冷启动最多等 180 秒并报进度。
2. **不再把 DSH 的命拴在自己身上**：以前把 DSH 的输出接在**管道**上，救星一退出（关窗/被删/崩溃），
   DSH 再往 stdout 写就可能拿到 EPIPE 而挂掉 —— 这是「用了之后 DSH 崩了」的机理之一。
   现在改成让 cmd **直接重定向到文件**，全程不经管道：救星随时可以死，DSH 照跑。
3. **新增 `--classify`（帮别人看日志）与 `--start`（命令行启动并等它就绪）**，并补一条故障类
   「插件依赖解析不到（少了包 / 插件在 profile 外面）」—— 这类**重装 DSH 治不了**，
   正解是把插件装进 profile。
4. **包内新增两份**：`给别的AI用的_DSH排障指令.md`（可直接转给任何模型或别人）、
   `tools\兼容性验证.ps1`（自证「救星开着不动你任何配置」——实测 1161 个配置文件 0 改动）。

### v4.5.1 验收（都在最终这份 exe 上跑过）

- `--selftest` 失败 0 / 85；`--configtest` 通过 21 / 0；`--maxcycle 4` 无漂移；`--start` 退出码 0
- 包内自检同样 0 / 85；文件清单逐条复算一致；桌面 exe = 包内 exe = zip 内 exe
- 隐私扫描（用户名/个人路径）0 命中；文档一致性检查 0 处问题

## ✅ v4.5（2026-09-18 主人封版 · 发人用这一版）

> **这一版就是 v4.5** —— 由上一条「装后体检 + 起不来根因分类器」起头，之后又陆续收了
> 皮肤、启动速度、换皮肤对话框、本地插件体检等一批修复。下面按顺序排，**越靠前越新**。
> 想知道自己手里是哪一版：看窗口标题栏（写着 `大肥鱼救星 v4.5`），或跑一次
> `大肥鱼救星.exe --selftest`，第一行就是 `== 大肥鱼救星 v4.5 自检 ==`。

### v4.5 追加（2026-09-18）

1. **本地插件体检（新判据，补一个真盲区）**：以前"Dsh 里登记了插件、可插件目录只有一半
   （比如缺 `package.json`、或者 `main` 指向的入口文件不在）"，救星的体检会**全绿**
   —— 因为悬空判定只问"这个 id 能不能解析到一个目录"，而静态探测只遍历
   profile `package.json` 的 dependencies（本地插件不在名单里）。
   现在「🩺 装后体检」里多一条**本地插件体检**，逐个本地插件查：
   `package.json` 在不在 / 能不能读 / 有没有 `name` / `main` 入口文件是否存在 / **有没有被补丁登记**；
   并把"半装（缺 package.json）"和"入口缺失"报红、"装了但没登记"报成提醒。
   判据双向验过：拿两个一次性假插件（一个缺 package.json、一个 main 指向不存在的文件）
   实测**会报红并点名**，撤掉后回到全绿。
2. **换皮肤对话框"按了没效果"修好了**：原来只有**双击**列表那一行才有效，
   点确定按钮走的是 WinForms 的 `DialogResult=OK` 这条路 —— 对话框正常关闭、调用方也收到 OK，
   但"选中的是哪一行"始终是空的，于是被当成"取消"，什么都不做。
   现在按钮自己交出选择，并加了回归判据守着（自检里那条"按确定能拿到 id"）。
   顺带把确定按钮的下边距放宽（原来几乎贴着底边），并恢复完整的五条可选皮肤。
3. **打开时不"半成品"了**：以前窗口出现后有 0.4~0.6 秒是**一个一个控件慢慢画出来**的
   （看着像白屏/闪）。两处根因：①头部横幅的缓存是在"窗口已经可见"之后才第一次画的，
   而显示阶段框架会连着做几次尺寸修正，缓存被反复丢弃重建；②几十个控件逐个首绘。
   现在：显示之前先把横幅画进缓存、尺寸变化改成防抖重建，并给主窗口加了
   `WS_EX_COMPOSITED`（整窗连同子控件先画进离屏缓冲再一次性合成）
   ⇒ 窗口出现的第一帧就是完整的。
4. **启动可见时间从约 1.5 秒降到约 0.3 秒**：根因是构造函数里那一句
   `ActiveRootUrl` 触发了"发现 dsh 在哪个端口"（首次约 1 秒），而它在窗口显示之前。
   现在状态栏先写"正在检测 dsh 状态…"，检测挪到窗口显示之后再跑。
5. **判据本身的两处修正**（免得报告时红时绿）：
   - `--paintbench` 原来量"窗口重绘一次多少 ms"，这个数**被外部负载支配**
     （机器上放着视频时，窗口往返本身就要 30ms，缓存版和现算版分不出来 ⇒ 假红）。
     现在改成**在内存里量"画这条路本身"**：贴缓存 vs 现算，判**比值 ≥3 倍**（实测约 9~12 倍）。
   - 日志黑匣子原先"超过 200KB 就直接不写了" ⇒ 改成**轮转**；
     命令行开关在"已有实例在跑"时原先**静默什么都不做** ⇒ 现在会打一行说明。

### 装后体检 + 起不来根因分类器（2026-09-17，v4.5 的起头）

> 这一版回答一个问题：装完／升级完插件之后出了毛病，到底该点哪个按钮、为什么。

### 为什么做这一版（两个被当场点出的缺陷）

1. **「🚨 排查崩溃插件 / ⛔ 禁用崩溃插件」有真盲区**：它只读**救星自己启动实例时写的那两份日志**。
   可 DSH 常常是被**桌面图标 / 可靠启动器 / 工坊桌面端**拉起来的 —— 崩溃行落在**别的文件**里，
   于是这两个按钮永远报「未能定位」「无需禁用」，**点了等于没点**。
2. **「🔧 插件兼容性检查」覆盖面比名字窄**：只看 `dependencies` 一层 ——
   不查 `bundles` 层（工坊聚合包、皮肤三件套都在这里）、不查"列了但没装"、不查重复条目，也不碰模型配置。

### 新增能力：一次跑完的「🩺 装后体检」（全部只读）

| 编号 | 检查 | 它回答什么 |
|---|---|---|
| A1 | **起不来根因分类器（7 类）** | 按 DSH 源码原文报错归类：悬空引用 / 重复 entry id / 入口缺失 / **等不到服务** / **端口冲突（受害者）** / 资源缺失 / 记忆花括号；每类给证据行 + "该点哪个按钮" |
| A2 | **日志来源全景** | 候选日志逐份列出（救星 / 可靠启动器 / 工坊桌面端 / 其它含关键字的日志），**写明读了哪几个文件** |
| A3 | **进程退出判据** | 日志尾部出现 `Node.js vXX` 崩溃页脚 ⇒ 判定"进程已崩溃退出"，与"还在跑但卡"严格区分 |
| A4 | ★ **页面地址 × 服务端口 × API 存活** | 直接解释那句 `Failed to fetch (gateway/internal)`：**它＝浏览器连不上本地服务**（客户端 `transportError`），不是插件冲突、也不是模型 API |
| A5 | **模型通路体检** | 各 provider 的 `baseURL` 逐个 TCP 探测（**不发密钥、不发请求**）+ 默认模型指向校验 |
| B1 | **三方对齐** | bundles ↔ dependencies ↔ 磁盘："列了没装"（悬空，会让整棵插件树崩）/ "装了没列"（隐形） |
| B2 | **重复条目 / entry id 冲突** | 同一文件内重复 id = 会触发硬抛错；跨层同 id = 需人工确认；备份副本是否被引用 |
| B3 | **多皮肤系统并存** | 有几个皮肤在生效（皮肤管理器不计入），有互斥风险就报出来 —— **只报，不替你关** |

并修掉 **C2 那个真 bug**：「排查崩溃插件」现在读 **4 类日志来源**；"没定位到"时会说明读了哪几份、
日志里是否其实有失败记录（不再让人以为工具失灵）。

### 还带了一个**可逆**的修复动作（C1）

体检若在 `bundles / dependencies` 里发现**悬空引用**（"删了插件、引用还在"），会**逐个问你**是否移除，
每一步都：**先打快照 → 只改那两处 → 写后校验 JSON → 再复验名字是否真的没了**。
硬边界写在代码里：**能解析到实体的项一律拒绝执行**（绝不误删其实装着的包），
**快照失败就中止**（没有退路不动手），命令行等价：`大肥鱼救星.exe --fixdangling <包名>`。

还有配套的 **C4：恢复被禁用的条目** —— 当根因分类判出「**等不到服务**」（某个插件在等被禁用插件提供的服务）时，
可以一键把它 `disabled: true` 改回 `false`（同样先快照、只动那一行、改后验 YAML；**不会碰到下一个条目的 disabled**）。
命令行等价：`大肥鱼救星.exe --enableentry <条目id>` / `--disableentry <条目id>`。

### ★ 向社区学习后的一次**安全升级**（2026-09-17，本节请重点看）

联网核对别人的真实报错时，发现了三个专门做这事的社区项目，其中
[`zhao1012/dsh-fix-duplicate-loader-id`](https://github.com/zhao1012/dsh-fix-duplicate-loader-id)
把 **DSH 的补丁语义**讲得非常清楚，也证明**我们原来的去重修法是错的**：

| 写法 | DSH 语义 | 冲突行为 |
|---|---|---|
| `- insert:` 子项 | **创建** row | 同一 id 创建两次 ⇒ 抛 `duplicate loader entry id`，**整树中止** |
| 顶层 `- id:`（无 insert） | **按 id 修改已存在 row** | 目标缺失只 warn，**不崩** |

所以正解是：把**后插入的那份**重复子项**转成顶层 id-patch**（并**删掉 `name:` 行**——带 name 且与目标不符会被 warn 跳过、补丁不生效）。
我原来的做法是"保留第一条、**删掉后面的块**" ⇒ **启动是修好了，但用户原来那块里的 config 被悄悄丢了**。

**本次升级**：
1. **🚫 去重重复条目** 改成 id-patch 转换（保留 config、剔除 name、幂等、无关条目不动）；
2. **判据收紧**：只有「`- insert:` **子项**里同 id 出现两次以上」才算冲突（与 loader 崩溃语义严格一致）；
   顶层 `- id:` 重复**不再**虚报为冲突；
3. **EACCES 分类 + 端口保留区间体检**（见下）；
4. 修完提示里写明"**可能复发**"（装/更新插件会重写补丁层，官方讨论 #3263）。

**没做的（诚实说）**：会话文件损坏的深度修复**没有自己造**——社区已有专门工具
[`dsh-session-surgeon`](https://github.com/xiaoshenming/dsh-session-surgeon)（覆盖 seq 断档、zstd 截断、孤立代理项等 8 类，
默认 dry-run、先备份、绝不凭空补 seq）。我们的做法是**在症状速查里直接指路**，而不是做一个半成品。

### ★★ 集大成版（2026-09-17 晚，把社区长处 + 我们的判据并到一个总控里）

| 编号 | 新增 | 说明 |
|---|---|---|
| **G1** | **全补丁层重复 insert 扫描** | 不再只看 home + web 两个文件：扫 **home 补丁 + 每个 profile 的补丁 + 每个已装包自带的补丁 + 本地插件补丁**（社区 skill 指出的真凶就是"多个 bundle 层各自 insert 同一个 id"） |
| **G2** | ★ **会话深度体检（已吸收，不再只是指路）** | 随包携带一个**纯 Node 助手**（`tools\session-inspect.cjs`，零依赖、只读）：**逐帧解码多帧 zstd**（Node 自带解压只解第一帧，跨帧会报 `Unknown frame descriptor`）→ 统计帧数/坏帧/尾部半截、行数与类型直方图、**seq 断档**、缺 message id、**悬空 tool/call**、孤儿 `.tmp`。判据是**实测校准过的**：打包行占 `1+dt.length`（148,383 行实测断档 0；换别的模型虚报 4~8 万处） |
| **G3** | **已知修复登记 + 复发复查** | 修完自动登记（去重/悬空引用）；以后每次装后体检**复查它还在不在** —— 因为装/更新插件会重写补丁层（官方 #3263），被撤销时直接提示重打 |
| **G4** | **社区工具指路表** | 明确写清"什么情况该找谁"（会话→session-surgeon；想固化→fix-duplicate-loader-id / pnpm patch；想常驻→dsh-refix；客户端白屏冲突→先判层+逐个禁用定位）——**会在报告末尾如实告诉用户我们做不了什么** |

`--selftest` 随之升到 **0 / 23**（新增"补丁层重复 insert""已知修复复查"两项）；
`harness_scenes` 升到 **44 / 44**（新增 zstd 魔数识别正负样本 4 例 + 全补丁层扫描冒烟）。

### 又补了三件（F 系列）
| 编号 | 能力 | 说明 |
|---|---|---|
| **F1** | **一键重装插件** | 「入口缺失」那一类的修法。只重装**已在你清单里**的插件（不当"任意包安装器"）、包名过格式校验、先快照、命令输出原样回显、复验入口真的回来了。`--reinstall <包名>` |
| **F2** | **分层判定**（哪一层出的问题） | L1 本地服务 / L2 插件树·配置 / L3 记忆注入 / L4 模型通路 / L5 环境；逐层给"证据 + 下一步"。★ 可直接指给它看**别人的日志**：`--layercheck <文件>` |
| **F3** | **启动失败 → 回滚建议** | 判据＝"有致命类加载失败 **且** 配置在最近快照之后被改过" ⇒ 建议先回滚；否则明说"不用回滚"（不制造无谓操作） |

### 验收（都在本机跑过，可复现）

- `--selftest` **0 / 20**（原 13 项 + 新增 7 项）
- `--configtest` **21 / 0**（无回归）
- 新专项 `_verify\harness_v45.cs`：对着**已编译的 exe** 反射调真码，**37 / 37**
  - A1 七类**正样本** + 3 条**负样本**（正常日志行必须"未归类"）
  - A3 带/不带崩溃页脚的正反样本
  - A4 注入假地址 ⇒ 必须判出"地址不一致"；并验证 **token 已脱敏**
  - A5 坏域名不可达 / 正常域名可达（阳性对照）
  - ★ B1 **假阳性回归测试**：`@deepseek-ai/dsh-base`、`dsh-web-app` **不得**被判悬空
  - C1 纯文本变换 5 例：中间项 / **末项（尾逗号必须修掉，否则 JSON 坏）** / dependencies 末行 / 负样本
  - C4 纯文本变换 4 例：禁用→启用 / 已是目标状态不动 / **不越界改到下一个条目的 disabled** / 找不到 id 不动
  - C5 两例 / C6 四例 / 同文件重复 id 清单两例
- 新专项 `_verify\harness_scenes.cs`（**跨环境故障场景**）：**21 / 21**
  - 七种"别人机器才有"的故障形态（报错文本抄自 DSH 源码）全部认出
  - 一份**健康日志**当负样本：**0 误报**
  - A3 进程退出判据 7/7 + 负样本
  - 与**真实隔离实验**日志对照：认出「悬空引用 + 包名」
  - F2/F3：花括号计数 3 例、回滚判据 4 例（真值表）、分层判定集成 3 例
- ★ **隔离 profile 对照实验**（`DSH_HOME` 指向临时目录，与真机完全隔离）：
  两个 profile **只差** bundles 里多一个不存在的包 ⇒ 实验组**在 compose 阶段就退出**、端口从未监听；
  对照组 5 秒内起来并监听。**把"删了插件、引用还在 ⇒ 起不来"从推测变成实测。**
- 真机演练：本机 45KB 真实崩溃日志被正确判为「**5 次插件树加载失败、全部属端口冲突、进程已崩溃退出**」

### 已知边界（诚实写在这里）

- 桌面副本（`桌面\大肥鱼救星.exe`）本次**因 GUI 正在运行、文件被占用而未能覆盖**；构建产物与包内 exe 不受影响。
- 「等不到服务」「重复 entry id」目前有**判据**但没有**一键修**（同样出于安全考虑：这两类都能"改错就起不来"）。
- C1 只处理 `bundles` 与 `dependencies` 两处；**不删任何包目录**，也不碰 `cordis.patch.yml`。

### 升级方式

直接替换 `大肥鱼救星.exe`。装完插件后**点一次「🩺 装后体检」**；命令行等价：`大肥鱼救星.exe --plugincheck`。

---

## v4.4（2026-09-16）

**这一版只做一件事：让工具在「DSH 不在 3080 端口」的机器上也能正确工作。**

### 为什么

DSH 默认端口是 3080，但**"有人的是 3081"不是故障**，而是同一套机制的必然结果：
可靠启动器是「**从 3080 起找第一个空闲端口**」（3080 被残留 node 或别的程序占着，就用 3081、3082…），
桌面图标的 `launcher.ini` 里也可能写死别的端口。

旧版的端口发现**只认 3080-3179 这个区间**。一旦你的 DSH 落在这个区间之外，
工具会退回 3080 ⇒ 停止 / 重启 / 打开 / 诊断**全部指向错的端口**，
表现出来就是"明明在跑，工具却说没起来"。

### 改了什么（4 条）

| 改动 | 说明 |
|---|---|
| ★ **显式端口设置** | 新增优先级最高的开关：环境变量 `DSH_PORT`，或 `port.txt`（只写端口号，支持 `#` 注释） |
| ★ **身份扫描：区间外也认** | 不再只看端口号 —— 扫描**所有**监听端口，用「该端口的进程命令行是否真的指向 dsh 入口」判身份，落在默认区间之外也能认出来 |
| **桌面图标端口纳入发现** | 没在运行时，"该起在哪"以 `launcher.ini` 的 `--port` 为准（免得图标认 3081、工具却起在 3080） |
| **报告里写明"凭什么"** | 自检 / 诊断 / 导出报告都新增一行「端口发现：当前用 X（来源：…）」；端口跑到区间外、又不是显式指定或身份认出时，给 WARN |

### 验收（都在本机跑过，可复现）

- `--selftest` **0 / 13**（新增的一项就报端口与来源）
- `--configtest` **21 / 0**
- 新增专项验证 `_verify\harness_port.cs` —— 对着**已编译的 exe** 反射调用真码，**10 / 10**：
  - 显式指定三条路径（环境变量 / `port.txt` / 忽略注释行）全部生效；
  - 区间外端口：**身份匹配才接受、身份不匹配则拒绝**（不会把别人的服务当成 dsh）；
  - 默认区间内的宽容行为**保持不变**（旧行为不被破坏）。
- 真机演练：把 `port.txt` 写成 `3081` ⇒ 界面地址立刻变成 `http://127.0.0.1:3081/`、
  来源标注 `显式设置（port.txt）`；删掉该文件后自动回到 3080。

**已知边界（诚实写在这里）**：「区间外」那条分支的**端到端**演练没法在开发机上做 ——
要让它跑到，就得把开发机自己的 dsh 挪到区间外（会断掉正在用的会话）。
所以它目前是**判据级验证 + 代码审查**，不是端到端跑通。

### 升级方式

直接替换 `大肥鱼救星.exe` 即可，**默认不需要做任何设置**。
想固定端口、或确认它认对了端口，看 `使用说明.md` 第九节。

---

## v4.3（2026-09-16）

**这一版只做一件事：把「会让 DSH 直接起不来」的那类故障，也纳入补丁体检。**

### 为什么加这一项

「补丁体检」原先查 3 项（`CREATE_NO_WINDOW` / `windowsHide` / pyyaml），全是**弹窗类**问题。
但还有一类**严重得多**的补丁：

> `@deepseek-ai/dsh-system-prompt` 把每个 prompt 段当模板插值，且**对未知引用刻意抛错**。
> 而 `dsh-memory-evolve` 会把你的**记忆原文**注入进去 ⇒ 只要记忆里出现一句
> `workflow 须含 {{prompt}} 占位符`，就会抛 `unknown prompt variable` ⇒ **DSH 启动失败**。

（这条是 2026-09-10 由**外部**排查出来的，不是我查的 —— 当时为它做了两层防线。）

### 新增的第 ④ 项

**「prompt 组装的字节防线」**，检查四个点并**逐项说明缺哪个**：

| 检查点 | 说明 |
|---|---|
| 上游转义 `index.js` | `dsh-memory-evolve` 的 `renderSnapshot()` 是否已把 `{{` 转义成 `{ {` |
| 上游转义 `prompts.js` | 同上，`renderInjectionSnapshot()` |
| 守卫插件已安装 | `profiles/web/plugins/dsh-prompt-guard/lib/index.js` 是否存在 |
| 守卫已登记到 patch | 它的 `insert` 行是否还在 `cordis.patch.yml` 里（**会被别的写者覆盖掉**） |

并且**能一键重打**：
- 两处转义 —— 用与上游同款的**锚点定点替换**（找不到锚点就**如实报"未改动"**，绝不瞎改）
- 守卫登记行 —— 缺了就补回，**热生效**（`patchReload: live`）
- 顺带扫一遍**会被注入的记忆文件**（`KEY.md` / 全局 `MEMORY.md` / `USER.md`）里还有几处字面量 `{{`，
  提醒你"治本"（把它们改成 `{ {` 就不必再依赖任何补丁）

### 顺带修掉一个假阳性（★ 这个更值得说）

为守卫补上登记行之后，`--configtest` **从 21/0 掉成 20/1**，报
「检测到 1 行完全重复的顶层条目（疑似拼接/追加失控）」——**而文件是完全合法的 YAML**。

根因是我们的自检按「**整行文本**」去重，而 `- insert:` 是**结构行不是条目身份**：
一个合法补丁里本来就可以有多条 `- insert:`（每条插一个插件）。
**更危险的是**：这个假阳性会触发"体检 → 自动回滚"，差点把刚做对的那次改动回滚掉。

已修：只对**带 `id` 的条目行**去重 ⇒ 真出现"整块被复制两遍"时照样能抓到，
而多条 `insert` 不再误报。**验收：`--configtest` 21/0，且"同一块重复应判非法"那条测试仍通过。**

### 升级方式

直接替换 `大肥鱼救星.exe` 即可。**建议升级后点一次「🧩 补丁体检」**，看看你的第 ④ 项是不是绿的。

---

## v4.2（2026-09-16）—— 「全量修复版」

**这一版把此前两份审查报告里记录的问题**（`_按键体检报告.md` 的 13 条 P 系列 +
`_按键理论审查.md` 的 34 条 T 系列，共约 36 个独立条目）**逐条修完并逐条复核**。

> 修完的标准不是"看着改对了"，而是：**每条都要有改前/改后的代码证据**、
> **重新构建**、**四套 CLI 自检全过**，能实测的还要**实测**。

### 最该先说的三条（都跟"安全 / 信任"有关）

| # | 问题 | 修法 |
|---|---|---|
| **T1/P12** | 「强力自愈」释放端口时**不校验进程身份就 Kill** —— 而且这个无校验的 Kill 恰好只在"会误杀"的场景才开火（因为前一步已经把 dsh 杀光了，剩下的占用者几乎必然**不是** dsh） | 统一走身份校验；**只结束命令行确实指向 dsh 入口的进程** |
| **T5** | "dsh 进程活着但端口没起来"这个状态**永远表达不出来** ⇒ 一键修复会判定"没跑"，然后在**同一个端口上再起一个实例**（进程重复、端口竞争、日志两份） | 修正状态语义：`存在 dsh 进程` ≠ `端口有人监听`；补充"救星启动过的子进程"与全量扫描两条来源 |
| **T10** | 「排查崩溃插件」的正则要求包名里含 `dsh` ⇒ 像 `argo-search` 这样**名字里没有 dsh** 的插件**永远检测不到** | 改为**对着实际安装清单白名单化**（package.json / node_modules），既收进 `argo-search`，又把 `cordis:include` 这类内置项挡在外面 |

### 其它修复（按主题）

**A. 不会误杀、不会误报**
- **T15** 两套互不一致的"什么算 dsh"判据（一个过宽、一个过松）→ 统一为"入口路径必须落在 dsh 目录下"
- **T14** 端口误认：区间里**别的 node 服务**会被当成 dsh → 追加身份校验
- **T11/T11b** 「关闭界面」把**已请求数**当**已关闭数**报出；且只要窗口标题含 DSH 字样就关，**会连带关掉同窗口里的其它标签页** → 改为投递后**复查确认**，且**多标签页的窗口一律不关**
- **T4/T4b/P11** 权限不足导致杀不掉时被 `catch {}` 静默 ⇒ 界面谎报"未找到运行中的 dsh 进程"（**假阴性 + 误导方向**）→ 失败原因如实上报，结论行按真实原因生成

**B. 不会在没有退路时动手**
- **T3** 「停止服务」**不判断快照是否创建成功**就强杀 → 失败必须明说"这次没有回滚退路"
- **T24** 「恢复配置」同样：自动快照失败**不中止** → 改为**中止**（口径与「修复插件」一致）
- **T16** `launcher.ini` 用 `File.WriteAllText`（会留下 0 字节/半截文件 —— 正是配置保险箱注释里点名禁止的写法）→ 改原子替换，并保留原 BOM 状态

**C. 不会假成功**
- **T13** 「一键修复」成功后回"正在尝试打开界面……"，但 `StartDsh` 只保证**进程已创建**；冷启动要 40~60 秒 ⇒ 探测必失败 ⇒ 浏览器**根本不会打开**（教科书式"点了没反应"）→ 如实说明"已拉起、尚未就绪"，且**不再立刻去开浏览器**
- **T6** 端口上已有实例在跑、但四个来源都没有有效 token 时，**必然白等 150 秒** → 立刻快失败并给出准确原因
- **T21** 「开机自启」只按"文件在不在"判成功 ⇒ `.lnk` 已存在但这次没写成功时**误报已启用** → 改为**回读快捷方式实际目标**并比对
- **T19** 「修复皮肤互斥」第一层写成、第二层失败时**直接返回**，留下**两层不一致的半套补丁** → 自动回滚第一层
- **T22** 「恢复配置」会覆盖**正在运行**的 DSH 的活动配置，而 DSH 之后写盘会把旧值**静默覆盖回去** → 明示"必须重启才生效"，并提供"顺手重启"选项

**D. 行为与按钮名一致**
- **T18/T18b** ★「修复皮肤互斥」原来把参数**写死成 official** ⇒ 点一下就把你正在用的**女仆皮肤关掉、换回官方外观**；顺带**凭空新增**一条"皮肤管理器"并把可能被关掉的它打开 → 现在**探测当前皮肤并保持它不变**，不凭空新增条目，且加确认框
- **T29** 确认策略倒挂：破坏性更小的按钮都有确认，破坏性最大的反而没有 → 给 一键修复 / 禁用崩溃插件 / 修复皮肤互斥 / 开机自启 补上确认

**E. 性能与观感**
- **T8** 「停止服务」的等待循环最坏 **165 秒**（20 轮 × powershell+WMI，每次超时 8 秒）且期间禁用所有按钮，而按钮上写着"预计 2~5 秒" → 改成**零进程创建**的托管轮询，上限 8 秒
- **T7** 「启动并打开」的"已等待 N 秒"进度**根本没接**（两处调用都传 `null`）⇒ 最长 150 秒里界面只有"⏳ 正在执行…" → 接到界面日志框
- **T31** 中文乱码的残留边界：取的是**当前区域性**的 OEM 代码页（多语言机器上会给 437 而实际是 936）→ 改为**强制子进程按 UTF-8 输出 + 固定按 UTF-8 解**
- **P6** 冷启动那行打印出 `[OK]   [OK]   pyyaml…`（两层前缀）→ 去掉内层
- **P8** 诊断文案里写死的"3080"→ 一律取运行时实际端口

**F. 其它**
- **T20/P10** 「打开日志」只开**一个**来源（当前实例可能是另一条启动路径起的），且失败时**完全无声** → 先判定"当前实例的日志在哪"，失败必须给出反馈，并列出全部来源
- **T17** 日志文件建不出来时，异常会把"读流"一起吞掉 ⇒ 子进程**永久阻塞在 write 上**（表现为"启动了但服务起不来"，日志 0 字节）→ 落盘与排空分离；顺带修了从不解进程句柄的泄漏
- **T23** 恢复快照时**从不校验** `MANIFEST.txt` 里记录的 `DSH home` ⇒ 从别的机器拷来的快照可零警告覆盖本机 → 不一致就明确警告
- **T27** 「恢复配置」名字像"恢复一切"，实际只覆盖 6 个关键文件 → 把范围**逐条列出来**
- **T27b** 没有单实例保护（能双开 ⇒ 两个进程并发写配置）→ 加全局互斥体，第二次双击会把已有窗口唤到前台
- **P9** 「导出诊断包」漏在忙锁数组外（26/27）→ 补上
- **T12** 「一键修复」失败时把精心拼出的真原因整段丢掉 → 带上
- **T25** "请点『打开无痕』"这条建议**自指死循环**（用户可能正是点了它才走到那）→ 改文案
- **T26** 「打开无痕」用的是**弱判据**（只看状态码），而"启动并打开"用强判据 ⇒ 端口坐着别的 HTTP 服务时会在**非 DSH 页面**上开窗并报成功 → 两处对齐
- **P15/P14** 快照会**自动轮换删除**、恢复对话框只列 10 份而列表显示 20 份 → 都写清楚

### 验收（都在本机跑过，可复现）

```
build.ps1            → 编译无 error CS，exe 1,137,152 字节（含立绘资源）
--selftest           → 失败项 0 / 12
--configtest         → 通过 21 / 失败 0
--envcheck           → exit 0
--diagnose           → exit 0，报告内无 [FAIL]，端口为运行时实际端口
GUI 冒烟              → 启动正常；空闲 15 秒内**子进程数 = 0**（轮询不创建进程，弹窗不复发）
单实例                → 第二个实例自动退出，计数保持 1
T31 编码              → 原始字节 E7 88 B6 3D（UTF-8 的"父="），按 UTF-8 解码正确
T10 白名单            → 真实日志 15 行 loader entry 全部正确排除；已装插件（含 argo-search）全部可检出
```

### 升级方式

直接替换 `大肥鱼救星.exe` 即可（配置与快照都不用动）。

---

## v4.1（2026-09-16）

**修复了一个会白白浪费你 5 分钟的问题** —— 「启动并打开」曾经卡 **316 秒**（约 5 分半）。

### 修了什么

**症状**：点「🚀 启动并打开」，界面卡住不动，等到超时才报"未能取得认证地址（已等 150 秒）"。

**根因（两层，第二层才是决定性的）**：

1. **读错了行** —— 抓认证地址时读的是一个**追加写入**的历史日志（跨 7 次启动共 15 行），
   而代码取的是**第一条**（早已失效的老 token），不是**最后一条**（当前有效的）。
2. **验证逻辑永远失败** —— 校验"这个地址是不是真的 DSH 页面"时，**没有带 cookie 容器**，
   而带 token 访问会返回 303 重定向 + `set-cookie`；不带 cookie 跟随后必然 401
   ⇒ 校验函数**永远返回 false** ⇒ 于是**永远判定"没有活着的实例"** ⇒ 去启动新实例
   ⇒ 而端口**已被占用**，新实例起不来 ⇒ 白等 150 秒，再 fallback 又 150 秒 = **316 秒**。

**修法**：①从最后一条往回找（并跳过端口已死的候选）②校验时带上 cookie 容器
③端口已被占用时**不再另起实例**，直接复用 ④新实例若提前退出**立刻报错**（不再干等），
并把它的 stderr 落盘（以前是读出来直接丢掉，所以日志是空的、毫无线索）。

**验收**：无窗口自检从「暂时没有可用认证地址」变成「**找到可用认证地址（来源：dsh-web.out.log）**」。
（另有两个实验佐证：同一 token，不带 cookie 容器 → 抛 401；带上 → 200 且页面含 `id="root"`。）

### 保持不变（有意为之）

- 点「🌐 打开无痕」的行为**没有变**（仍是无痕窗口）—— 虽然修好校验后会走"应用模式窗口"分支，
  但按钮写着"无痕"，就给你无痕。

### 升级方式

直接替换 `大肥鱼救星.exe` 即可（配置与快照都不用动）。

---

## v4.0（2026-09-16）

- 新增「环境与弹窗」组：🧩 补丁体检 / 🔔 弹窗体检 / 🖥 默认终端
- 新增配置保险箱：🛡 备份配置 / ✅ 配置自检 / 📚 快照列表 / ♻ 恢复配置
- 端口动态发现（不再硬编码 3081 —— 那次硬编码导致"按钮全部打空"）
- 认证地址等待上限 30 秒 → 150 秒（冷启动本来就要 40~60 秒）

## v3.x 及更早

- 五项诊断、插件兼容性排查、崩溃插件定位与禁用、皮肤互斥修复
- 脱敏诊断包导出

---

*本文件随包分发；打包脚本会把它一并放进发布目录。*
